import { EnumTradutorPipe } from './enum-tradutor.pipe';

describe('EnumTradutorPipe', () => {
  it('create an instance', () => {
    const pipe = new EnumTradutorPipe();
    expect(pipe).toBeTruthy();
  });
});
